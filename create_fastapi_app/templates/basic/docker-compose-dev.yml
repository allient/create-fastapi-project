version: '3.8'

services:
  fastapi_server:
    build: .
    restart: always
    command: "sh -c 'uvicorn app.main:app --reload --workers 3 --host 0.0.0.0 --port 8000'"
    volumes:
      - ./app:/code
    expose:
      - 8000
    ports:
      - 8000:8000      
    env_file: ".env"

